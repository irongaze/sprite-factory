## Manages a set of controls to allow setting project-wide color palette
@tool
extends Window

# Called when the node enters the scene tree for the first time.
func _ready():
  #close_requested.connect(func(): hide())
  pass
